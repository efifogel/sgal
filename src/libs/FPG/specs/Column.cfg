[DEFAULT]
name = Column

[class]
base = Shape
public_functions = ["clean_geometry"]

[clean_geometry]
desc = Clean the geometry.
signature = virtual void clean_geometry();

[fields]
spec = %(name)s_fields_spec.txt
exec = Node::bounding_sphere_changed
